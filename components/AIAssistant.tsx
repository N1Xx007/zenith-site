
import React from 'react';

/**
 * Component removed as per user request to redirect buttons directly to sections
 * instead of opening a chat assistant.
 */
const AIAssistant: React.FC = () => {
  return null;
};

export default AIAssistant;
